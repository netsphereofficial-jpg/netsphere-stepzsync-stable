package com.health.stepzsync.stepzsync

import io.flutter.embedding.android.FlutterFragmentActivity

class MainActivity : FlutterFragmentActivity() {
    // Plugin registration is handled automatically by Flutter
    // Removing manual registration to prevent "Attempted to register plugin" warnings
}
